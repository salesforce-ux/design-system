import CodeView from '../../../shared/components/CodeView';
import { getDisplayElementById } from '../../shared/helpers';
import * as Base from './base/example';

<div className="doc lead">
  A styled checkable input group that communicates if an option(s) are true, false or indeterminate
</div>

<CodeView exampleOnly>
  {Base.default}
</CodeView>

## About Checkbox-button-group

The checkbox-button-group is a set of [checkbox buttons](/components/checkbox-button) wrapped in a fieldset that is also a [form element](/components/form-element). It is described by a legend with the classes `slds-form-element__legend` and `slds-form-element__label` applied to it.

## Base
<CodeView>
  {Base.default}
</CodeView>

## States

### Has Error
<CodeView>
  { getDisplayElementById(Base.states, 'has-error') }
</CodeView>

### Disabled
<CodeView>
  { getDisplayElementById(Base.states, 'disabled') }
</CodeView>
