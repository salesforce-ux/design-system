import CodeView from '../../../shared/components/CodeView';
import CodeBlock from '../../../shared/components/CodeBlock';
import Blockquote from '../../../shared/components/Blockquote';
import { getDisplayElementById } from '../../shared/helpers';
import * as RadioButtonGroupExamples from './base/example';

<div className="lead doc">
  A styled select list that can have a single entry checked at any one time.
</div>

## Default

<CodeView>{RadioButtonGroupExamples.default}</CodeView>

### Variations

#### Stacked

A stacked modifier that accounts for the limited width of mobile devices. It's recommended to use the stacked modifier when you have 3 or more options.

<Blockquote type="note" header="Mobile Only">
  The <code>.slds-radio_button-group_stacked</code> modifier only affects mobile
  devices, all other devices will show the default layout.
</Blockquote>

<CodeView>
  {getDisplayElementById(RadioButtonGroupExamples.examples, 'touch-stacked')}
</CodeView>

### States

#### Disabled

<CodeView>
  {getDisplayElementById(RadioButtonGroupExamples.states, 'disabled')}
</CodeView>

#### Required

<CodeView>
  {getDisplayElementById(RadioButtonGroupExamples.states, 'required')}
</CodeView>

#### Has Error

<CodeView>
  {getDisplayElementById(RadioButtonGroupExamples.states, 'error')}
</CodeView>
